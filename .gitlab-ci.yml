image: node:latest

stages:
  - test

playwright_tests:
  stage: test
  script:
    - npm ci
    - npx playwright test
  artifacts:
    when: always
    paths:
      - tests-results/
    expire_in: 1 day
  only:
    - main